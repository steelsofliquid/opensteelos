# Pre-compiled components
This content is pre-compiled for those who may have difficulty compiling the project on their own.

For reference, `opensteelcore.bin` is the base kernel binary, while `OpenSteelOS.iso` is a bootable disc image created with grub-mkrescue and packaged with the GRUB bootloader.
